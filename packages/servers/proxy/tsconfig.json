{
	"extends": "tsconfig/nest.json",

	"compilerOptions": {
		"outDir": "./dist",

		"baseUrl": "src",
		"paths": {
			"AppModule": ["AppModule"],

			"@middlewares": ["middlewares"],
			"@middlewares/*": ["middlewares/*"]
		}
	},

	"include": ["./src/**/*"]
}
